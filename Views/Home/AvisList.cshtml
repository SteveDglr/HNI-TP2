@model List<TPLOCAL1.Models.Opinion>

<title>HN - TP LOCAL</title>
<header>
    <img src="~/resources/GroupeHN.png" class="image" alt="GroupeHN" />
    <h1>Liste des Avis</h1>
    <link rel="stylesheet" href="~/resources/Index.css" />
</header>

<body>
    <nav>
        <ul>
            <li><a href="/Home">Acceuil</a></li>
            <li><a href="/Home/Index/Form">Remplir le formulaire</a></li>
        </ul>
    </nav>


    <table>
        <thead>
            <tr>
                <th>Nom</th>
                <th>Prénom</th>
                <th>Avis donné</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var opinion in Model)
            {
                <tr>
                    <td>@opinion.LastName</td>
                    <td>@opinion.FirstName</td>
                    <td class="@(opinion.OpinionGiven == "O" ? "green" : "red")">
                        @if (opinion.OpinionGiven == "O")
                        {
                            <span>Oui</span>
                        }
                        else
                        {
                            <span>Non</span>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
</body>

